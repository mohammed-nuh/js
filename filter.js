let priceOfProducts = [1050, 500, 1400, 250];
let filteredProducts = priceOfProducts.filter(product => product >= 1000);

console.log(`priceOfProducts array => ${priceOfProducts}`);
console.log(`filteredProducts array => ${filteredProducts}`);